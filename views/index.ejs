<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Tarefas</title>
</head>
<body>
    <h1>LISTA DE TAREFAS</h1>
    
    <p> <% let x = 2*4 %>   <%= x %> </p>
    <% if (x >= 10) { %>
        <p> <%= soma  %>  </p>
    <% } %>

    <ul>
        <% for (tarefa of tarefas) { %>
            <li> 
                <% if (tarefa.feito) { %>
                    <del> <%= tarefa.texto %> | 
                        <%= tarefa.dataCriacao.toLocaleDateString('pt-BR') %> 
                    </del>
                <% } else { %> 
                    <%= tarefa.texto %> | 
                    <%= tarefa.dataCriacao.toLocaleDateString('pt-BR') %> 
                <% } %>   
            </li>
        <% } %>
    </ul>


</body>
</html>